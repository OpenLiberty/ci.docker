Open Liberty kernel image for Docker

The Dockerfile in this directory is used to build the open-liberty:kernel image on Docker Hub. The image contains Open Liberty Kernel and an IBM Java Runtime Environment.

Usage

Instructions for using the image can be found on Docker Hub. It is possible to build the image yourself by cloning this repository, changing to the release/kernel directory and then issuing the command docker build ..
