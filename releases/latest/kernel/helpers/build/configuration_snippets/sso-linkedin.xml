<server>    
    <featureManager>       
        <feature>appSecurity-3.0</feature>
        <feature>socialLogin-1.0</feature>
    </featureManager> 
 
   <!-- defaults from the server metatype.xml are declared as variables here
         If nothing is passed in elsewhere, the defaults will prevail.
         Required parameters have no defaults, so are not defined here.
         The required parameters are 
           clientId, clientSecret, redirectToRPHostandPort
    -->
    <variable name="sec_sso_linkedin_mapToUserRegistry" defaultValue="false"/>       
 
    <sec_sso_linkedin_
        clientId="${sec_sso_linkedin_clientId}"
        clientSecret="${sec_sso_linkedin_clientSecret}"
        redirectToRPHostAndPort="${sec_sso_linkedin_redirectToRPHostAndPort}"  
        mapToUserRegistry="${sec_sso_linkedin_mapToUserRegistry}"       
    ></sec_sso_linkedin_>
</server>