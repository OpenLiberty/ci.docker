<server>
    
	<featureManager>       
        <feature>appSecurity-3.0</feature>
        <feature>socialLogin-1.0</feature>
    </featureManager> 
    
    <!-- defaults from the server metatype.xml are declared as variables here
         If nothing is passed in elsewhere, the defaults will prevail.
         Required parameters have no defaults, so are not defined here.
         The required parameters are 
           clientId, clientSecret, redirectToRPHostandPort,
           tokenEndpoint, authorizationEndpoint
    -->
    <variable name="oauth2Login.groupNameAttribute" defaultValue=""/>
    <variable name="oauth2Login.userNameAttribute" defaultValue="email"/>
    <variable name="oauth2Login.displayName" defaultValue="oauthLogin"/>
    <variable name="oauth2Login.userApi" defaultValue=""/>
    <variable name="oauth2Login.mapToUserRegistry" defaultValue="false"/>
    <variable name="oauth2Login.realmNameAttribute" defaultValue=""/>
    <variable name="oauth2Login.scope" defaultValue=""/>
    <variable name="oauth2Login.tokenEndpointAuthMethod" defaultValue="client_secret_post"/>
    <variable name="oauth2Login.accessTokenHeaderName" defaultValue=""/>
    <variable name="oauth2Login.accessTokenRequired" defaultValue="false"/>
    <variable name="oauth2Login.userApiType" defaultValue="basic" />

    <!-- the id attribute will not be substituted  -->
	<oauth2Login
		id="oauth2Login"		
        clientId="${oauth2Login.clientId}"
        clientSecret="${oauth2Login.clientSecret}"
        redirectToRPHostAndPort="${oauth2Login.redirectToRPHostAndPort}"
        tokenEndpoint="${oauth2Login.tokenEndpoint}"
        authorizationEndpoint="${oauth2Login.authorizationEndpoint}"
        userAPI="${oauth2Login.userApi}"
        groupNameAttribute="${oauth2Login.groupNameAttribute}"
        userNameAttribute="${oauth2Login.userNameAttribute}"
        displayName="${oauth2Login.displayName}"        
        mapToUserRegistry="${oauth2Login.mapToUserRegistry}"
        realmNameAttribute="${oauth2Login.realmNameAttribute}"
        scope="${oauth2Login.scope}"
        tokenEndpointAuthMethod="${oauth2Login.tokenEndpointAuthMethod}"
        accessTokenHeaderName="${oauth2Login.accessTokenHeaderName}"
        accessTokenRequired="${oauth2Login.accessTokenRequired}"
        userApiType="${oauth2Login.userApiType}"
	></oauth2Login>
</server>